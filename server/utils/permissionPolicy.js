/**
 * @deprecated Use domains/permissions/policy/permissionPolicy.js directly.
 * Thin shim for backward compatibility during Phase 5 migration.
 */
const policy = require('../domains/permissions/policy/permissionPolicy');
module.exports = policy;
