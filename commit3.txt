feat: Add file preview functionality with modal dialog

- Implement FilePreviewDialog component for in-browser previews
  * Support image formats (JPG, PNG, GIF, BMP, WebP, SVG)
  * Support video formats (MP4, WebM, OGG, MOV)
  * Support audio formats (MP3, WAV, OGG, AAC, M4A)
  * Support PDF documents with inline viewer
  * Support text files with syntax highlighting
- Add download mode differentiation (inline vs attachment)
  * Preview requests use 'inline' disposition
  * Download requests use 'attachment' disposition
  * Set appropriate MIME types for inline display
- Implement clean UI for non-previewable files
  * Show file type and format information
  * Display download button with user guidance
- Optimize dialog UX
  * Remove redundant buttons (keep only header icons)
  * Add loading states and error handling
  * Prevent infinite loops in useEffect
  * Auto-cleanup Object URLs to prevent memory leaks

Features:
- Click file to preview (if supported) or see info modal
- Download button in dialog header
- Responsive modal sizing based on content type

